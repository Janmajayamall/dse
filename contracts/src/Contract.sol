contract TimeLockedWallet {

    uint epochDuration;
    address owner;

    constructor(
        uint _epochDuration,
        address _owner,
    ) {
        epochDuration = _epochDuration;
        
    }

    // need a way to deposit
    // 50% go to wallet balance & 50% for security deposit

    // owner 

    // epoch & start the epoch 

    // 

} 